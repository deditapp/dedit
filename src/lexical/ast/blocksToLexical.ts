import { EditorState } from "lexical";

import { Document } from "@dedit/models/dist/v1";

/**
 * Convert a Dedit document into Lexical editor state.
 */
export const blocksToLexical = (document: Document): EditorState => {};
