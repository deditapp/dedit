import { EditorState } from "lexical";

import { Document } from "@dedit/models/dist/v1";

/**
 * Convert Lexical editor state into Dedit blocks.
 */
export const blocksToLexical = (state: EditorState): Document => {};
